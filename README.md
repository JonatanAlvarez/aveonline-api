# Go + Gin + GORM

## Dependencies
  - PostgreSQL

## Getting started
  - Create postgres database
  - Fill the `.env` file
  - Install dependencies
  ```sh
    go mod download
  ``` 
  - Launch a development server with 
  ```sh
    go run main.go
  ``` 